Puppet
------
	- check fetch res with byte, blob, buffer...
	- add generate sha1 integrity
	- add 1 time startup data response (motd, upload, list url...)
	- add auto load feature for mainSites scrolling
	- add filter to url object in json list
	- change app.js log func switch type to switch code
	- change code 5 (startup) to 1
	- add tfe feature to resample and zip into package
	- insert random buffer to upload

TFE
------
	- fix broken symbolic link not show in File Explorer
	- missing move to previous location
	- add compress and extract .gz.xz
	- auto prefix number order
	- parse option argument
	- add list scroll
	- add compile to .jar
	- add run ext .jar

	BUG:
		- 2 option with long name trimmed (same name) error when press arrow up/down

Magick
----------
	- Batch convert images (directory to output directory)
		magick mogrify -path outputDir -resample 10% *.jpg
		magick -size 128x128 xc:none -fill "#1e1e1e" -draw "roundrectangle 0,0 127,127 32,32" \( -background none input.svg -resize 118x118 \) -gravity center -composite output.png

EBook
------
    - Web APIs:
        - missing link: https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/ArrayBuffer
----‐---------------------------------------------------------------‐----‐------------

Vim
-------------
    done:
        - fix 3 key combo  for uppper case  shortcut to full upper (space+U+I)
        - missing space+num copy selected space to slot
        - space+space match space+y
        - space+t+u match upper/lower case
        - space+a+space ? slot (alt+v)
        - add a space after comment
        - V) End button back 1 char
        - I) Home End button
        - Alt-v-clipboard slot
        - N) Alt-End not config
        - V) duplicate selection to next line
        - alt-v-up|down look like alt-v-left|right
        - V) alt-c-up|down copy selected to top|bottom
        - create tag pair
        - shortcut install plugin, coc
        - V) quote with space (space-q-l)

check already remap or not
